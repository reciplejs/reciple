/**
 * Hello world description wkwkwk
 * ```ts
 * console.log(add([1, 2]));
 * ```
 * @param param0 Hiii
 * @returns Hello
 * @deprecated
 */
export function add([num1, num2]: [number, number]) {
    return num1 + num2;
}
